{
  "name": "eslint-config-ibi",
  "version": "1.0.1",
  "description": "eslint config for IBI Data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "push-all": "git push -u origin master && git push -u production master && npm publish",
    "push-wifi": "git push -u github master"
  },
  "author": "IBI Data <admin@ibidata.com>",
  "license": "ISC",
  "dependencies": {
    "eslint": "^3.15.0",
    "eslint-config-airbnb-base": "^11.1.0",
    "eslint-plugin-import": "^2.2.0"
  }
}
